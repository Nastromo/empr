export const collapsePatient = (bool) => ({
    type: 'COLLAPSE_PATIENT',
    bool
});

export const collapseGrossing = (bool) => ({
    type: 'COLLAPSE_GROSSING',
    bool
});

export const collapseInfo = (bool) => ({
    type: 'COLLAPSE_INFO',
    bool
});