export const setCells = (text) => ({
    type: `SET_CELLS`,
    text
});

export const setChrome = (text) => ({
    type: `SET_CHROME`,
    text
});

export const setZero = (text) => ({
    type: `SET_ZERO`,
    text
});